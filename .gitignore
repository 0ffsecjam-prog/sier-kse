# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (never commit secrets)
.env
.env.local
.env.*.local
.env.docker

# vercel
.vercel

# typescript
*.tsbuildinfo

# persistent data (DB + uploads) — host bind mounts
/data/postgres/*
/data/uploads/*
!/data/postgres/.gitkeep
!/data/uploads/.gitkeep

# prisma generated client artifacts that come from migrate (keep migrations/)
